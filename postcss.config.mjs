/*This file is a configuration file for postcss, which is a tool that is used to process css with different plugins, such as tailwind*/ 
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
