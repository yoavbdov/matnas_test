/*
  SIDEBAR — the left navigation panel, visible on every page (except login).

  Shows:
  - App logo / name at the top
  - Navigation links:
      לוח בקרה     → /dashboard
      תלמידים      → /students
      חוגים        → /classes
      מדריכים      → /teachers
      חדרים        → /rooms
      לוח זמנים    → /schedule
      דוחות        → /reports
      הגדרות       → /settings
  - Active page is highlighted
  - "התנתקות" (logout) button at the bottom → calls Firebase signOut

  Stays the same width always. No collapse on this version.
*/
