/*
  BTN — styled button wrapper.

  Props:
  - variant : "primary" (teal) | "secondary" (gray) | "danger" (red) | "ghost" (no fill)
  - onClick  : click handler
  - disabled : disables + grays out
  - loading  : shows a spinner, disables the button
  - children : button label

  Thin wrapper around shadcn Button.
  Used everywhere so button styles are consistent across the app.
*/
