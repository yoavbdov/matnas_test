/*
  FIELD — wraps any form input with a label and optional hint.

  Props:
  - label    : text shown above the input
  - hint     : small gray text shown below (e.g. "10 ספרות בלבד")
  - required : shows a red asterisk next to the label
  - children : the actual input element (text, select, date, etc.)

  This is purely a layout wrapper — it doesn't know what's inside it.
  Using it makes all form fields look consistent without repeating label styling.
*/
