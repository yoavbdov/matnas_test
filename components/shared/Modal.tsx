/*
  MODAL — reusable popup/overlay wrapper.

  Props:
  - title   : text shown in the modal header
  - onClose : called when user clicks X or clicks outside
  - size    : "sm" | "md" | "lg" | "xl" — controls max width
  - children: whatever content the modal contains

  Renders a centered dialog with a dark overlay behind it.
  Press Escape also closes it.

  Every form/detail popup in the app uses this as its wrapper.
*/
