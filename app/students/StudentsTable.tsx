/*
  STUDENTS TABLE — shows the filtered/sorted list of students.

  Props:
  - students   : filtered + sorted array of Child
  - enrollments: all enrollments (to count classes per student)
  - onRowClick : called with a Child when a row is clicked → opens detail modal

  Columns:
    שם מלא | מגדר | כיתה | דירוג ישראלי | טלפון איש קשר | חוגים רשומים | סטטוס

  Uses the shared Table component.
  Status shown as a Badge (green = פעיל, gray = לא פעיל).
*/
